{
  "name": "proton-drive-desktop-root",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "compile:fuse": "CLI_APP_VERSION_NAME=external-drive-linux_desktop bun --cwd sdk/js/cli build:fuse",
    "compile:sync": "CLI_APP_VERSION_NAME=external-drive-linux_desktop bun --cwd sdk/js/cli build:sync"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
