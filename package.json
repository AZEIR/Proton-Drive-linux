{
  "name": "proton-drive-desktop-root",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "compile:fuse": "bun --cwd sdk/js/cli build:fuse",
    "compile:sync": "bun --cwd sdk/js/cli build:sync"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
