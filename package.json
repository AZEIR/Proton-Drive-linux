{
  "name": "proton-drive-desktop-root",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "build:fuse": "bun --cwd sdk/js/cli build:fuse",
    "build:sync": "bun --cwd sdk/js/cli build:sync"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
