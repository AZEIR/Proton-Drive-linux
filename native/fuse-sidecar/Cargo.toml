[package]
name = "drive-fuse-sidecar"
version = "0.1.0"
edition = "2024"
license = "GPL-3.0-only"
publish = false

[dependencies]
fuser = { version = "0.18.0", features = ["libfuse3"] }
libc = "0.2"

[profile.release]
lto = true
codegen-units = 1
panic = "abort"
strip = true
